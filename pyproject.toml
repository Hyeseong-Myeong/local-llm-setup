[tool.ruff]
line-length = 120
target-version = "py311"
exclude = ["archive", "venv", ".venv", "__pycache__"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
# E402: 각 스크립트가 sys.path.append(...)로 상위 디렉토리를 모듈 검색 경로에
# 추가한 뒤 import하는 의도된 패턴을 쓰고 있어 제외한다.
ignore = ["E402"]
